{
  "name": "tav",
  "version": "0.4.0",
  "description": "Conversation bookmarks for Claude Code — navigable anchor points in long sessions",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "@types/node": "^22.19.10",
    "typescript": "^5.9.3"
  }
}
